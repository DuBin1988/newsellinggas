1 "分类报表"中打印时，只打印了部分信息，没有把报表统计的全部信息打印；
处理： 已处理

2 “小区营业报表”中统计的户数总和，与 档案信息查询中的户数总和不一致；
处理：应该是界面上的sql统计的方式不对，
     现场处理
     

3 “小区营业报表”中查询出来的数据中，同一个小区出现好几个，小区名称写的不一致；
处理： 现场处理数据

4 “小区营业报表”是否要增加到产品中，李主任说他们以前两个系统时，使用这一的统计方式，新系统中他用这个报表主要看总户数；
处理：与何总商量产品中是否要增加，麻城以前也提过这样的报表；

5 “可疑用户报表”中查询条件下拉选择中有个是空的；
处理：现场添加；

6 “网点报表”中，最后一行增加字符“交款人：”、“收款人：” ，每天结账打印后，交款人、收款人都要在相应位置签字确认；
处理： 已处理

7 “网点报表”中，用A4打印时，没有打印全，报表下方的合计等信息没有打印上；
处理： 已处理

8 “网点报表”中，分类统计中的“非民用”分类是空的，没有显示出统计数据；
处理： 已处理

9 “网点报表”中，“售气笔数、销售气量、实收金额”等一行，在细分为
   “卡表收费笔数、卡表售气量、卡表售气金额” ，
   “机表收费笔数、机表销售气量、机表实收金额” ；
处理：已处理

10 “网点报表”中，“补卡数”细分为“收费补卡数、不收费补卡数”；
处理：已处理

11 “网点报表”中，“换表笔数、换表补气量、换表卡费”没有统计出来，实际这几项有数据，从换表信息中可以查到；
处理：已处理

12 “网点报表”中，“民用发卡数、民用发卡费、非民用发卡数、非民用发卡费”统计的不对，从发卡信息中可以查到；
处理： 已处理

13 “网点报表”中，把金额是否可以分为“转账、现金”，（是否可以只在总金额上区分，因为全部区分界面上可能会很乱）
处理：

14 “网点报表”中，“机表用户上次结余、机表用户本次结余”；
       增加所有用户的“本期结余、本期欠费”，要进行分类，按照民用、非民用、卡表、机表；
        目的是了解每天结余、欠费的情况
处理：已处理

15 “网点报表”中，统计了报建笔数、报建金额 是测试的，需要从库中清除；
处理：现场处理；

16 “换表信息列表”中的“补气量、剩余补气量”都是一样的，这是不对的，落实情况；
处理：现场；

17 “网点报表”中，“机表预存笔数、机表预存金额”不好定义，先放着；
处理：

18 在档案信息列表中，把账户余额合计，放着列表下边；
处理：已处理

增加机表费用减免



//---辛钊

1 档案变更 中数字类型的字段都是“数据类型错误”，保存后，一直提示“正在工作”；
处理：ok，hibernate拷贝过来就好了；

2 新添加的4个修改功能对应的查询中，应该是显示变更字段的内容，如果没有变更的字段就不要给查询中显示了；
      现在全部显示，变更前后值都一样，找个变更的内容半天看不出来；
      （商量是否要做）
处理： 刘磊协商

3 进入档案变更信息查看功能报错，退出了系统；
处理：

4 抄表录入修改，修改气量后，点击保存，左边界面刷新了，但是气量还是修改前的；
      在抄表信息中查询，发现没有修改成功（确实产生了修改记录，但是抄表信息中气量没有变成修改后的）；
处理：ok

5 发卡售气修改时，如果修改发卡气量，就报错，退出系统；
处理：

6 其他收费修改，要可以对“有效”字段进行修改；
处理：

7 其他收费修改查询，发卡售气修改查询、 条件上增加操作日期段；
处理：


这个功能的界面应该有，只是没有把功能链接添加，

1 档案变更 中数字类型的字段都是“数据类型错误”；
处理：
8 收费记录修改 需要对 用户编号、用户姓名、小区名称、地址、联系方式 都要能修改；
处理：
闻喜遇到换表功能问题，产品中可能也存在，
用户用A表厂的表，卡里的基本信息klx,dqdm,yhh,bjql,czsx,tzed,kmm写到用户档案了；
A表坏或建错后，换表操作为B表，但是 档案中A表的klx,dqdm,yhh,bjql,czsx,tzed,kmm这些信息没有跟换成B表的，
导致B表换表补气出来的卡接表不认；


