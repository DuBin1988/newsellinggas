
1、数据查询----用户档案信息，显示列表中，非民用户 用气设备 没有显示出来；

2、数据查询----用户档案信息，查看详细信息时，民用、非民用户 证件类型 值为 气表厂家了。

3、数据查询----用户档案信息，查看详细信息时，非民用户，用户姓名 取成 单位名称了，应取 联系人值；小区名称在此 应取 单位名称，目前为空。

4、数据查询----用户档案信息，查看详细信息时，气表厂家值是TJ，其他气表类型等均无值。

5、数据查询----用户档案信息，查看详细信息时，用户设备 没有非民用设备，只是民用的。

6、用户建档----非民用建档，抄表员应该在抄表录入后，得值，在此不应可操作。（最后抄表日期同样存在）

7、用户建档----非民用建档，基表品牌、基表型号、基表厂家在此无用，可以去掉。

8、用户建档----非民用建档，开户日期为非输入框，也没有默认值，单位管理里也没有开户日期，所以开户日期就没有值了。

9、用户建档----档案变更，输入用户编号选择字段保存（这时界面全部清空），再另选择一个字段，变更前内容还是前一个字段内容。

10、换表管理----换表登记，保存后弹出 是否打印框，出现了3次，前二次提示框字显示 是否打印发票，最后一次提示框显示 是否打印购气证。 

11、换表管理----换表登记，输入信息保存时，打印选择 否，提示框显示了2次。

12、换表管理----换表补气，在左边列表查询不出换表登记记录，无法进行补气。

13、数据查询----换表信息，卡厅审核人之后无用的列可以去掉。

14、数据查询----换表信息，时间段查询，写的是操作日期，过滤值是按 换表日期过滤。如果按操作日期查询会少记录，操作日期与换表日期有不一样的情况。

15、换表管理----换表撤销，左边列表翻页不可用。

16、数据查询中有 换表信息，换表管理中也有 换表信息查询 两个是否可以合成一个。或者将 换表信息 去掉。

17、售气收费----机表交费 上次总累计购气量与表上次累计购气量值相等了。

18、机表管理----民用抄表批量录入，本次底数等值没有保存到数据库。同一个用户录入二次，录入不同本次底数，第三次查询出数据，上次底数 仍然是 0（每次录入前 都进行抄表单生成操作）.

19、数据查询----机表抄表信息，列表下面的 用气量、用气金额 没有合计。


